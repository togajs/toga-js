/**
 * # Title
 *
 * Long description that spans multiple
 * lines and even has other markdown
 * type things.
 *
 * Like more paragraphs.
 *
 * * Like
 * * Lists
 *
 *     var code = 'samples';
 *
 * @param {Type} name Description.
 * @param {Type} name Description that is really long
 *   and wraps to other lines.
 * @param {Type} name Description that is really long
 *   and wraps to other lines.
 *
 *   And has line breaks, etc.
 *
 * @example
 *
 *     var foo = 'bar';
 *
 * @tag Foo bar.
 * @tag
 */

/**
# Title

Long description that spans multiple
lines and even has other markdown
type things.

Like more paragraphs.

* Like
* Lists

    var code = 'samples';

@param {Type} name Description.
@param {Type} name Description that is really long
  and wraps to other lines.
@param {Type} name Description that is really long
  and wraps to other lines.

  And has line breaks, etc.

@example

    var foo = 'bar';

@tag Foo bar.
@tag
 */

/**
    # Title

    Long description that spans multiple
    lines and even has other markdown
    type things.

    Like more paragraphs.

    * Like
    * Lists

        var code = 'samples';

    @param {Type} name Description.
    @param {Type} name Description that is really long
      and wraps to other lines.
    @param {Type} name Description that is really long
      and wraps to other lines.

      And has line breaks, etc.

    @example

        var foo = 'bar';

    @tag Foo bar.
    @tag
 */

    /**
     * # Title
     *
     * Long description that spans multiple
     * lines and even has other markdown
     * type things.
     *
     * Like more paragraphs.
     *
     * * Like
     * * Lists
     *
     *     var code = 'samples';
     *
     * @param {Type} name Description.
     * @param {Type} name Description that is really long
     *   and wraps to other lines.
     * @param {Type} name Description that is really long
     *   and wraps to other lines.
     *
     *   And has line breaks, etc.
     *
     * @example
     *
     *     var foo = 'bar';
     *
     * @tag Foo bar.
     * @tag
     */

    /**
    # Title

    Long description that spans multiple
    lines and even has other markdown
    type things.

    Like more paragraphs.

    * Like
    * Lists

        var code = 'samples';

    @param {Type} name Description.
    @param {Type} name Description that is really long
      and wraps to other lines.
    @param {Type} name Description that is really long
      and wraps to other lines.

      And has line breaks, etc.

    @example

        var foo = 'bar';

    @tag Foo bar.
    @tag
     */

    /**
        # Title

        Long description that spans multiple
        lines and even has other markdown
        type things.

        Like more paragraphs.

        * Like
        * Lists

            var code = 'samples';

        @param {Type} name Description.
        @param {Type} name Description that is really long
          and wraps to other lines.
        @param {Type} name Description that is really long
          and wraps to other lines.

          And has line breaks, etc.

        @example

            var foo = 'bar';

        @tag Foo bar.
        @tag
    */
